import { useCallback, useReducer } from 'react';

const formReducer = (state, action) => {
   switch (action.type)
};

export const useForm = (initialInputs: string, initialFormValidity: boolean) => {
   return
};

